# Заполнить нулями элементы квадратной матрицы, расположенные по ее периметру (использовать один цикл).
print('Задание 3.2')
import numpy as np
N = int(input('Введите число'))
A = np.random.randint(1, 100, size=(N, N))
print("Исходная матрица A:")
print(A)
for i in range(4 * N - 4):
    if i < N:
        A[0, i] = 0
    elif i < 2 * N - 1:
        A[i - (N - 1), N - 1] = 0
    elif i < 3 * N - 2:
        A[N - 1, N - 1 - (i - (2 * N - 1))] = 0
    else:
        A[N - 1 - (i - (3 * N - 2)), 0] = 0
print("Матрица A после замены элементов по периметру на нули:")
print(A)
